# Review worker session manifest (one per reviews/<stage>/round_NN/).
# Canonical evidence for reviewer/synthesis Hermes sessions — do not duplicate in artifacts.model_usage.
# Fill session_id and usage_evidence after each worker session (dp_review_job.py --record-session).

review_stage: ""
round: 0
preset: ""

reviewers: {}
# Entries are generated by dp_review_job.py --init-manifest.

synthesis:
  action: review_synthesis_standard
  role: review_main
  hermes_profile: dp-review
  output: synthesis.md
  session_id: ""
  usage_evidence: ""
